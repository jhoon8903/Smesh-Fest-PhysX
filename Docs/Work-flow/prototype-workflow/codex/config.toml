# Inactive template. Merge into the project's .codex/config.toml when applying.
# Preserve existing unrelated settings. Model availability depends on the host.
model = "gpt-6-astra"
model_reasoning_effort = "ultra"

[agents]
enabled = true
max_concurrent_threads_per_session = 2
default_subagent_model = "gpt-5.6-terra"
default_subagent_reasoning_effort = "medium"
